//BTreeSet has the advantage of being sorted.
use rand::seq::SliceRandom;
use rand::thread_rng;
use std::collections::BTreeSet;



fn main() {
    let fruist: Vec<&str> =vec![
        "apple",
        "banana",
        "cherry",
        "date",
        "elderberry",
        "fig",
        "grape","honeydew"
    ];

    println!("Hello, world!");
}

